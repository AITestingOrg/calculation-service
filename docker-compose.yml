version: '3'
services:
  discoveryservice:
    image: aista/eureka
    ports:
      - "8761:8761"

  rabbitmq:
    image: rabbitmq:management
    ports:
      - "5672:5672"
      - "15672:15672"

  gmapsadapter:
    depends_on:
      - discoveryservice
      - rabbitmq
    image: aista/gmaps-adapter:local
    ports:
      - "8080"
    environment:
      - RABBIT_HOST=rabbitmq
      - EUREKA_SERVER=discoveryservice

  calculationservice:
    depends_on:
      - discoveryservice
      - rabbitmq
    image: aista/calculation-service:local
    ports:
      - "8080"
    environment:
      - RABBIT_HOST=rabbitmq
      - EUREKA_SERVER=discoveryservice

  notificationservice:
    depends_on:
      - discoveryservice
      - rabbitmq
    image: aista/notification-service:local
    command: notification-service
    ports:
      - "32700:32700"
    environment:
      - EUREKA_SERVER=discoveryservice
      - RABBIT_HOST=rabbitmq